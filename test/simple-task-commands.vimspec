Describe Simple Task Commands
    Before each
        call CreateSandbox()
    End
    It Runs tasks
        call themis#log('nvim ' . has('nvim') . ' Pyver ' . g:omnipytent_defaultPythonVersion)
        CreateTasksfile
        @task
        def foo(ctx):
            with open('foo', 'w') as f:
                f.write('hello')
        .

        OP foo
        Assert readfile('foo') == ['hello']
    End
End
